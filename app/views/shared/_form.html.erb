<%= form_with(model: company) do |f| %>
  <div class="form-group">
    <%= f.label :name, "Nombre de la empresa" %>
    <%= f.text_field :name, class:"form-control name-input" %>
  </div>
  <%= f.submit "Enviar", class:"btn btn-primary btn-sm" %>
<% end %>
